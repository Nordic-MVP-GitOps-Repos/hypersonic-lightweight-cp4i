apiVersion: eventstreams.ibm.com/v1beta2
kind: EventStreams
metadata:
  name: minimal-prod
spec:
  strimziOverrides:
    kafka:
      storage:
        class: ibmc-block-gold
    zookeeper:
      storage:
        class: ibmc-block-gold