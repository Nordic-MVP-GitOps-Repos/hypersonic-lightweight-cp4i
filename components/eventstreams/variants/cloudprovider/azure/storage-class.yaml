apiVersion: eventstreams.ibm.com/v1beta2
kind: EventStreams
metadata:
  name: eventstreams
  namespace: cp4i
spec:
  strimziOverrides:
    kafka:
      storage:
        class: managed-premium
    zookeeper:
      storage:
        class: managed-premium