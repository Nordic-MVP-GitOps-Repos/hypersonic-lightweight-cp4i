apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mq-operator
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "100"
  labels:
    group: applications
spec:
  destination:
    namespace: cp4i
    server: https://kubernetes.default.svc
  project: default
  source:
    path: components/mq/operator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true